var React = require('react');
var ReactDOM = require('react-dom');

var TableScreen = require('./components/TableScreen.jsx');

ReactDOM.render(<TableScreen />, document.getElementById('table-screen'));
